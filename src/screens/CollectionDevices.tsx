import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const CollectionDevices = () => {
  return (
    <View>
      <Text>CollectionDevices</Text>
    </View>
  );
};

export default CollectionDevices;

const styles = StyleSheet.create({});
